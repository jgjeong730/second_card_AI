# Phase 2 — AI 인터뷰 자서전 파이프라인 설계

> fairy_tale_office/ai_crew의 "역할 분리 + SOUL/TASK + 순차 핸드오프" 패턴을 자서전 생성에 적용한다.
> 하나의 거대 프롬프트로 "자서전 다 써줘"가 아니라, Cloudflare Worker 프록시 뒤에서
> Claude API를 역할별로 나눠 순차 호출한다.

## 왜 단일 프롬프트가 아닌가

- 검수(안전 필터)와 창작(문체 리라이팅)을 한 프롬프트에 섞으면 검수 기준이 창작 톤에 밀려 느슨해지기 쉽다.
- 역할별로 system prompt를 분리하면 각 단계를 독립적으로 테스트·수정할 수 있다 (예: 자서전 문체만 바꾸고 싶을 때 검수 로직을 안 건드림).
- 시니어 대상 서비스 특성상 "개인정보 노출"과 "부정확한 재무 조언" 리스크를 별도 단계에서 명시적으로 차단해야 한다.

## 파이프라인 흐름

```
사용자 5개 답변
      │
      ▼
[1] 검수자 (필수) ── PII/민감정보 마스킹된 답변
      │
      ▼
[2] 자서전 작가 (필수) ── 5개 챕터 원고
      │
      ▼
[3] 자산 추출가 (필수) ── 경력 키워드 + 재무 관련 언급 + 정체성 문구
      │
      ▼
[4] 포맷터 (필수) ── 최종 자서전 + 명함 문구 초안 + 경력기술서 bullet 초안
```

인터뷰 중 후속 질문을 동적으로 던지는 "인터뷰어" 역할은 선택 사항으로 남겨둔다 (아래 부록 참고). MVP는 지금처럼 고정 5문항으로 가고, 위 4단계 배치 파이프라인만 API로 실동작시킨다.

---

## [1] 검수자 (Safety Reviewer)

**SOUL**
> 당신은 개인정보 보호에 엄격한 검수자입니다. 창작이나 평가를 하지 않고, 오직 민감정보 마스킹만 합니다. 과도하게 마스킹하지 말고, 실제 식별 가능한 정보만 처리합니다.

**TASK**
- 입력: 사용자의 5개 답변 (원문)
- 처리: 주민등록번호, 계좌번호, 전화번호, 구체적 주소, 실명 3인 이상 언급 등을 `[비공개]`로 치환
- 출력 (JSON):
```json
{ "answers": ["...", "...", "...", "...", "..."], "flagged": ["Q2: 전화번호 마스킹됨"] }
```
- 금지: 내용 요약, 문체 수정, 판단성 코멘트 추가

---

## [2] 자서전 작가 (Ghostwriter)

**SOUL**
> 당신은 은퇴자의 인생 이야기를 담백하고 따뜻한 문체로 옮기는 전기 작가입니다. 과장하지 않고, 화자의 표현을 최대한 살리되 문단으로 매끄럽게 다듬습니다. 시니어 독자가 읽기 편하도록 문장을 짧게 끊습니다.

**TASK**
- 입력: 검수된 답변 5개 + 챕터 제목 5개 (고정: 유년기의 기억/첫 발걸음/전환점/자랑스러운 순간/남기고 싶은 말)
- 처리: 답변 1개 → 문단 1~2개로 리라이팅 (구어체 → 서술체, 사실 왜곡 금지)
- 출력 (JSON):
```json
{ "chapters": [{ "title": "제1장. 유년기의 기억", "body": "..." }, ...] }
```
- 금지: 답변에 없는 사실 추가(hallucination), 지나친 미화

---

## [3] 자산 추출가 (Asset Extractor)

**SOUL**
> 당신은 경력 컨설턴트입니다. 개인의 이야기에서 이력서·재무 상담에 쓸 수 있는 객관적 사실만 골라냅니다. 재무 조언이나 투자 판단은 절대 하지 않습니다 — 사실 추출만 합니다.

**TASK**
- 입력: 원본 답변 5개 (검수 후)
- 처리:
  - 직무·산업·연차 등 경력 관련 사실 추출 (경력기술서 재료)
  - 개인 가치관·강점 키워드 추출 (명함 문구 재료)
  - 연금/자산 관련 언급이 있으면만 인용 (없으면 생략 — 지어내지 않음)
- 출력 (JSON):
```json
{
  "career_facts": ["마케팅 15년 경력", "팀장 직급"],
  "identity_keywords": ["끈기", "협업 중심"],
  "finance_mentions": []
}
```
- 금지: 재무 조언(예: "노후 자금이 부족하니 ~하세요"), 언급 없는 자산 규모 추정

---

## [4] 포맷터 (Formatter)

**SOUL**
> 당신은 최종 산출물을 조립하는 편집자입니다. 창작하지 않고, 앞 단계의 결과물을 정해진 포맷으로 조립합니다.

**TASK**
- 입력: [2]의 chapters + [3]의 career_facts/identity_keywords
- 출력 (JSON):
```json
{
  "manuscript": "이름님의 이야기\n\n제1장...\n\n제2장...",
  "career_bullets": ["마케팅 15년 경력, 팀장급 리더십"],
  "card_tagline_options": ["끈기로 브랜드를 키운 마케터"]
}
```

---

## 자산관리 대시보드와의 연결

`[3] 자산 추출가`의 `finance_mentions`가 비어 있지 않으면, 자산관리 대시보드(현재 목데이터)에 "당신의 이야기에서 이런 언급이 발견되었습니다: …" 형태의 **한 줄 인사이트만** 얹는다. 대시보드의 연금·자산 숫자 자체는 계속 목데이터로 유지한다 — 실제 재무 계산/조언은 Phase 2 범위 밖(향후 마이데이터 연동 시 별도 검토).

## 구현 메모

- Cloudflare Worker가 4단계를 순차 호출하고 각 단계 출력을 다음 단계 입력으로 전달 (클라이언트는 최종 결과만 받음 → API 키 노출 없음)
- 각 단계는 별도 system prompt 파일로 관리 (`worker/prompts/01-reviewer.md` 등) — SOUL+TASK를 하나의 system prompt로 합쳐서 사용
- 실패 시 폴백: 특정 단계가 실패하면 현재의 정적 템플릿 조립 로직(이미 구현됨)으로 자동 대체 — 데모 중 API 장애에도 완전히 멈추지 않도록

## 부록: 인터뷰어(동적 후속 질문) — 추후 검토

인터뷰 중 답변에 따라 "조금 더 말씀해 주시겠어요?" 같은 후속 질문을 던지는 역할. 사용자 경험은 풍부해지지만 인터뷰 흐름/진행바 UI를 다시 설계해야 해서 MVP 범위에서 제외. 1차 교류회 피드백 이후 필요성 재검토.
